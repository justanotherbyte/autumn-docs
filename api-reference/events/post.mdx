---
title: 'Send Event'
openapi: 'POST /events'
---

When you pass in a `customer_id` that doesn't exist, a new customer will be created. You can set the properties of this new customer (eg. `name`) through the `customer_data` field.